// Option B (local-only): auth footer hidden. Re-enable for Option A (Supabase auth).
interface Props { onSignOut: () => void }

export default function SidebarFooter(_props: Props) {
  return null
}
